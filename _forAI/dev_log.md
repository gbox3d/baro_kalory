# Dev Log

## 목차

- [Entries](#entries)

## Entries

> 최신 항목이 위.

### 2026-08-18 — 캘리브레이션: 옛 계약을 걷어내고, 라이브 뷰를 얹고, 발행이 실제로 적용되게 했다

백엔드가 **0.16.4** 로 올라가 있었다(배포기는 아직 0.16.2 — 두 갈래를 버전 문자열로 구분할 수
있는 것은 지금뿐이고, 언제든 다시 같아진다). `/api/help?format=json` 의 라우트 표면은 **105개로
완전히 동일**해서 목록만 비교하면 "차이 없음"으로 읽힌다. 갈린 것은 **의미**였다.

#### 1. `apply` 는 이제 확인이다 — 되돌리기는 재발행이다

`POST /profiles/camera/:id/apply` 가 0.16.2 의 "런타임에 적재"에서 0.16.4 의 **"확인"** 으로
바뀌었고, **옛 리비전을 실어 보내면 409** 다. 팀 보드 **#61** 이 이 저장소를 지목해 쓴 공지였다 —
*"If your UI has a revision picker wired to `apply`, that path is gone."*

리비전 드롭다운이 최신순이라, **맨 위 하나만 되고 그 아래는 전부 409** 였다. 고를 수 있는 것 중
유일하게 되는 것이 이미 적용돼 있는 것이었으니, 되돌리기라는 기능 자체가 죽어 있었다. 백엔드가
권하는 재발행 경로(같은 카메라에서 `fromRevision` 으로 copy)는 복사 패널이 자기 자신을 제외해
막고 있었다.

- `applyRevision(revision)` → `applyLatestToRuntime()` / `rollbackToRevision(rev)` 로 분리
- 되돌리기는 `POST /profiles/camera/:id/copy {from: 자기, fromRevision}` 으로 **재발행**
- 버튼이 무슨 일이 일어날지 말한다 — 최신이면 「지금 적용」, 옛것이면 「이 리비전으로 되돌리기」
- 응답 키를 단정하지 않는다(`?.` 체인) — 커밋 `33e5414` 의 방어적 읽기 선례
- `openApplyPanel` → `openRevisionPanel` 개명. 이 창은 더 이상 적용하지 않으므로
  (`suggestPorts` → `suggestPortsByConvention` 선례)

#### 2. 라이브 뷰 + 스윕 가시화

스윕은 실기를 20분 독점하는데 화면은 퍼센트만 셌다. "도는 중"과 "굳음"을 구분할 수 없고, 시작
전에는 카메라가 어디를 보는지도 모른 채 시작했다(「밝을 때, 무늬 있는 쪽」이 전제인데도).

재료는 이미 선에 있었다 — `status` 의 **`recent[]`**(마지막 6표본, `{zoom, dx, dy, residualX,
residualY, peak, margin, usable}`). **아무도 읽지 않던 필드**다.

공유 모듈 `camera-preview.mjs` 로 프리뷰를 붙이고 그 위에 SVG 오버레이를 얹었다 — 눌러 준 지점의
레티클, 중앙에서 뻗는 명령 벡터, **합격선 10px 을 잔차와 같은 배율로 그린 허용 원**, 남은 오차
벡터(합격선 넘으면 빨강), 못 쓴 표본의 파선, 지나온 표적을 잇는 자취, 줌 앵커 사다리, 삼분할선.
HUD 는 진행·경과/남음·기기·줌 앵커·잔차·`peak·margin`·쓸 만한 표본, 캡션은 서버 문장 그대로,
범례는 기호 설명(없으면 화려한 도형은 그냥 노이즈다).

- 좌표는 언제나 **1920×1080 논리 프레임** — `viewBox` 하나로 겹치고 `naturalWidth` 를 읽지 않는다.
  그래서 스테이지를 16:9 로 못박았다.
- **잔차는 ×8 로 확대해 그리되 배율을 화면에 적는다** — 합격선이 10px 이라 실척으로는 화면에서
  1~3px 이 되어 안 보인다. 말없이 부풀린 그림은 거짓말이다.
- 프리뷰는 기본 꺼짐·시작/정지·선택만 기억·`pagehide` 에 종료·정지는 await (저장소 계약).

화면을 실제로 몰아봐서 잡은 것 둘 — **`num` 중복 선언**으로 페이지 스크립트가 통째로 죽었고
(소스 정규식 테스트로는 못 잡는 종류), 16:9 스테이지가 넓은 단에서 세로로 자라 **검증·시작
버튼을 화면 밖으로 밀어냈다**. 높이를 `max-width: calc(42vh * 16/9)` 로 묶었다(비율은 유지해야
오버레이 매핑이 맞다).

#### 3. 발행이 실제로 적용되게 했다 — 사람을 pm2 로 보내지 않는다

이교수님이 실기 `cam-real-002` 로 20분 전체 스윕을 돌렸다. rev 1 이 발행됐는데
`live.optics` 는 `declared:false`, `/api/ptz` 에 `hfovDeg` 가 없었다. 드라이버가 **생성 시점
광학을 물고 있기** 때문이다. 그리고 화면은 `pm2 restart baro-backend` 를 안내하고 있었다.

**재시작은 필요 없었다.** 백엔드 문서가 `POST /api/cctv/active` 를 「with no restart」라고
적고 있고, mint 요약도 "restart **/device-select**" 라고 말한다. 실측:

```
POST /api/cctv/active {"id":"cam-real-002"}              -> {"unchanged":true}  광학 그대로
POST /api/cctv/active {"id":"cam-real-002","force":true} -> optics declared:true, points 14,
                                                            hfovRange [1.64, 57.3], drift []
```

**`force` 가 전부였다.** 같은 id 는 `force` 없이는 `{unchanged:true}` 로 튕기는데 그게 200 이라,
"카메라 다시 고르기"를 시도해 본 사람은 안 된다고 결론짓고 재시작으로 간다.

발행하는 **모든** 경로(mint · copy · import · 되돌리기 재발행)가 발행 직후
`applyPublishedToRuntime(id)` 를 부른다. `pm2 restart` 문구는 전부 사라졌다. 적재에 실패했을
때만 사람이 할 일을 말한다 — 발행은 이미 성공했으므로 그 사실까지 실패로 말하면 안 된다.

#### 4. "캘리브레이션 있나"의 출처를 바꿨다

같은 병이 한 군데 더 있었다. `loadCalibInstalled()` 이 `config.devices[].intrinsics` 로
판정하고 있었는데, 0.16.3 부터 발행은 거기에 **아무것도 쓰지 않는다**(씨앗값 전용). 그래서
적용이 끝난 카메라를 두고 「캘리브레이션이 없습니다」라고 말했다. 정본은 서버가 매 요청
계산해 주는 `live.optics` 다 — 이제 "발행본이 적용돼 있습니다 (14점 · 화각 1.64°~57.30°)".

씨앗값 분기도 갈라 적었다. **지우면 안 된다** — 벤더마다 줌 눈금이 달라(IDIS 는 배율 ×100,
100..1200) 씨앗값이 없으면 스윕이 남의 눈금으로 앵커를 양끝에 몰아 놓고도 rc=0 으로 끝난다.
측정된 것처럼 보이는 표가 남는다(보드 #61).

#### 5. 막히지 않은 것부터 — 씨앗값 경고 · 퇴역 문구 · 죽은 마운트 예시

「적용 해제」는 백엔드에 그 상태가 없어 요청으로 냈고(**#73**), 없는 라우트에 UI 를 미리 만들면
죽은 코드가 되므로 그것만 빼고 나머지를 처리했다.

- **설정 화면이 씨앗값을 지우게 두고 있었다.** 「캘리브레이션」 칸의 「보정 없음」을 고르면
  `d.intrinsics = ""` 로 **명시적 제거**가 나가는데, 안내는 "클릭 센터링이 보정 없이
  동작합니다"뿐이었다. 반쪽만 맞는 말이다 — 지금 조준에 쓰는 곡선은 발행본이라 그대로이고,
  지워지는 것은 **스윕이 줌 앵커를 잡는 기준**이다. 벤더마다 줌 눈금이 달라(IDIS 는 배율 ×100,
  100..1200) 씨앗값이 없으면 다음 캘리브레이션이 남의 눈금으로 앵커를 양끝에 몰아 놓고도
  **성공으로 끝난다.** 측정된 것처럼 보이는 표가 남아 사람이 알아챌 방법이 없다(보드 #61).
  라벨을 「광학 씨앗값」으로, 선택지를 「씨앗값 없음」으로 고치고, 고른 순간 경고색으로
  무슨 일이 일어나는지 적는다. 이 칸이 편집하는 것은 **적용된 곡선이 아니라 씨앗값**이라는
  사실이 화면에 드러나야 한다.
- **퇴역 안내가 재시작만 말했다.** 오늘 `pm2 restart` 를 전부 걷어내면서 이 한 줄을 빠뜨렸다.
  기기 재선택이 더 싼 길인데 그걸 안 알려주면 사람을 SSH 로 보낸다. **자동 적재는 일부러
  붙이지 않았다** — 발행과 달리 퇴역은 정리 동작이라 누른 즉시 광학이 사라지면 시연 중에
  놀랄 일이 되고, 「적용 해제」가 생기면 이 자리는 그쪽으로 간다(#73). 대신 언제·어떻게
  반영되는지를 정확히 적고, 되돌릴 방법이 API 에 없다는 것도 확인 문구에 넣었다.
- **`.env.example` 이 죽은 마운트를 예시로 썼다.** `/barocalory` 는 nginx 가 404 를 내는
  경로이고 다시는 응답하지 않는다(보드 #66) — 예시를 `/calory` 로 고쳤다. `cctv.html` 의
  스크린샷 파일명 접두사 `barocalory-` 는 경로가 아니라 파일 이름이라 그대로 둔다.

settings 0.4.0 → 0.5.0, calibration 0.7.0 → 0.7.1, 패키지 0.8.69. 테스트 116/116.

#### 6. 백엔드 응답 — 고쳐진 것, 우리에게 온 것, 되돌려 보낸 것

- **#71 의 1·3 이 고쳐져 도는 서버에 올라왔다**(`461ee69`). 디프가 아니라 실서버에서 확인했다 —
  `help/devices` 가 라우트 옆에 `force:true` 를 적고, `help/calibration` 의 재선택 언급 세 곳이
  전부 `{id, force:true}` 를 명시한다. 스윕 20분 끝에 찾아낸 함정이 이제 문서다. 2번(mint 후
  자동 적재)은 열려 있다 — **(b) `restartRequired` 개명은 지금 따로 가고 (a) 는 #73 과 같이 가라**고
  적었다. 따로 정하면 "발행은 항상 적용된다" 가 먼저 서고 #73 이 예외를 들고 와 규칙이 둘이 된다.
- **#76 — 발행본이 git 밖으로 나갔다**(`profiles/camera/` → `localfiles/profiles/camera/`).
  **우리 영향 없음**: 소스의 `profiles/camera/` 15곳은 전부 API 라우트이고 파일 경로가 아니다.
  `path`·`movedTo` 를 화면에 쓰지 않으므로 바뀐 두 문자열이 닿는 곳이 없다. 가정하지 않고 이전
  후 실측했다 — `live.optics {declared:true, points:14, hfovRangeDeg:[1.64,57.3]}`, `profileDrift []`.
- **#73 은 구현되지 않았다.** 백엔드가 요청에 동의한 뒤 「이교수님 결정」으로 열어 두었다.
  **되돌려 보냈다** — 계약 세부는 재료를 다 가진 두 저장소가 정할 일이고, 소유자를 거치면 하루짜리
  계약이 일정 문제가 된다(#76 본문과 #71 댓글에도 같은 미룸이 있었다). 세 질문에 소비자로서 직접
  답했다. **detach 를 「미설정」과 구분하는 비트(`live.optics.detached`)는 선호가 아니라 요구다** —
  없으면 비교 시연 중에 화면이 고장 경고로 덮이고, 없는 상태를 프론트가 추론하는 짓은
  `config.intrinsics` 를 거짓 음성으로 만든 바로 그 실수다.
- **#76 의 백업 공백에 이교수님 판단을 실었다.** git 과 그만 견주라는 것, 그리고 **소실보다 중복이
  나쁘다**는 것 — 잃은 발행본은 시끄럽지만(`declared:false`, 첫 화면에서 보인다) 중복된 발행본은
  조용하다. #61 사고에서 아무도 아무것도 잃지 않았다; 데이터는 두 벌 다 있었고 그게 문제였다.
  백업을 만든다면 **읽기 경로 밖 · 리비전으로 열거되지 않음 · 복원은 명시적 사람의 행위**라는
  제약이 붙는다. 자동으로 주워질 수 있으면 그건 백업이 아니라 두 번째 진실이다.
- **퇴역 버튼 툴팁이 남아 있었다.** 앞 커밋에서 확인창과 성공 문구만 고치고 `title` 을 빠뜨려,
  같은 화면이 같은 사실을 두 가지로 말하고 있었다. 문구를 맞췄다.

calibration 0.7.1 → 0.7.2, 패키지 0.8.70. 테스트 116/116.

#### 남은 것 · 보드

- **#71 [open]** 로 백엔드 이슈를 냈다 — 낡은 드리프트 안내문(`intrinsics 를 채우세요` 는
  #61 이 없앤 두-사본 설계의 조언이다) · mint 가 스스로 적재하거나 `restartRequired` 이름을
  정정할 것 · 같은 id 재선택에 `force` 가 필요하다는 문서화. **#61 에는 소비자 보고를 댓글**로.
- 리비전이 2개 이상인 분기는 실물로 못 눌러 봤다 — 지금 이 카메라의 발행본이 rev 1 하나뿐이다.
  새 테스트 단언이 그 계약을 잡고 있다.
- `pnpm test` 116/116.


### 2026-08-17 — 개발기 프런트를 pm2 에 올렸다 · Windows 는 부팅 훅이 없다

"지금 띄울 수 있나"에서 출발했는데 **이미 떠 있었다** — 맨 `node server.mjs`(PID 19408, 19:00
기동)가 8180 을 잡고 있었다. 그래서 새로 띄우는 대신 끝까지 확인하고, 규칙대로 pm2 에 옮겼다.

확인한 것: 페이지 8개 전부 200 · `app.css` 200 · 프록시가 `x-baro-upstream` 으로 백엔드를 말하고
`/api/version` → `backendVersion 0.16.2` · `/api/health` 가 `ok:true` 와 실제 PTZ 좌표
(`pan 7917 / tilt 2034`)를 내려 **실기까지 살아 있음**. 브라우저로도 몰아 `/cctv` 헤더 셀렉터에
등록 기기 5대가 백엔드에서 내려오는 것과 게이트 없음·카드 잠금 0 을 봤다.

**`ecosystem.config.cjs` 신설**(앱 이름 `calory-ui`). 설계 판단 하나가 중요하다 — **설정값을
이 파일에 두지 않았다.** pm2 는 등록 시점의 환경을 스냅샷해 매 기동 재주입하므로 `BARO_*` 를
넣으면 `.env` 를 고쳐도 안 듣고, 주소 사본이 `~/.pm2/dump.pm2` 로 굳는다(공개 저장소에 주소를
안 넣는 규칙이 저장소 밖에서 새는 셈). 팀 보드 **#56** 이 형제 프로젝트 dwarf 에서 이걸로 한
시간을 쓴 기록이라 그 함정을 피해 갔다. `interpreter: 'node'` 는 `.mjs` 가 pm2 매핑에 없어서
명시했다.

실측으로 확정한 것:

- **autorestart 동작** — PID 를 강제 종료하니 3초 뒤 새 PID 로 `↺ 1`, 라우트 다시 200.
- **`.env` → `pm2 restart` 로 반영됨** — 스냅샷을 비워 뒀기 때문이다. `pm2 jlist` 의 `pm2_env`
  에 `BARO_*` 없음 확인 → 백엔드 주소를 더미로 바꿔 재시작 → 기동 로그가 더미를 찍음 → 원복.
  #56 의 교훈("파일이 아니라 프로세스를 본다")을 그대로 적용했다.
- **덤프에 주소가 없음** — `dump.pm2` 를 파싱해 `BARO_*`·호스트명·포트 0건 확인.

**Windows 에는 pm2 부팅 훅이 없다.** `pm2 startup` 이 `Init system not found`(Startup.js:216)로
실패하고, 작업 스케줄러·Run 키·서비스·시작 폴더 어디에도 pm2 항목이 없다. 전역 npm 패키지도
`pm2` 하나뿐이다. 즉 **`pm2 save` 는 덤프를 쓰기만 하고 아무도 읽지 않는다** — 전역 규칙이
"save 하면 재부팅 후에도 남는다"고 읽히는데 이 머신에서는 절반이다. 스케줄러 자동화를 제안했고
**이교수님이 수동(`pm2 resurrect`)을 택했다.**

부수 발견: 브라우저 `localStorage.baro-api-base` 에 죽은 임시 터널 주소
(`*.trycloudflare.com`)가 남아 미연결 게이트가 뜬다. 서버 문제가 아니라 저장값 문제다 —
설정에서 `http://127.0.0.1:8180`(dev 서버가 프록시하므로 동일 출처)로 바꾸면 풀린다.
`?api=reset` 이 탈출구.

문서: `readme.md` 에 「상주 (pm2)」 절과 `.env` 설명 추가, `inventory.md` 에 파일·명령 추가
(Pages 워크플로 TODO 도 실제 `pages.yml` 로 확정), `memo.md` 에 「개발기 상주 (pm2)」 신설.

### 2026-08-15 — 시뮬레이터 다중 인스턴스 계약을 memo 에 기록

시뮬(baro_unreal) 쪽에서 한 기기 다중 인스턴스 운영이 확정되고 플러그인 v0.1.16 에 포트 커맨드라인
스위치(`-ScenePort` 등 3종)가 들어왔다. 이 저장소 관점의 요지: **인스턴스 관리(기동·포트 부여·
헬스체크·재시작)는 에이전트가 한다** — 계약은 "`-ScenePort=N` 기동 → `GET :N/scene/catalog` 200 →
프로세스 exit = 실패 감지"이고, 충돌이면 시뮬이 스스로 즉시 죽으므로 **살아 있으면 포트는 보장된다**.
인스턴스별로 `simulator.port` 와 devices 카메라 포트 블록을 전부 분리해야 한다(카메라 DELETE 후에도
CGI 리스너가 시뮬 프로세스 종료까지 남는 UE 제약). 상세는 memo 「시뮬레이터 인스턴스 계약 (다중 실행)」
과 실행 중 sim 의 `GET /scene/help` 「인스턴스 기동·포트 계약」.

### 2026-08-14 — 설정 탭에 LLM 카드 · 「연결」과 「동작」은 다른 질문이다

설정 페이지 0.3.4 → **0.4.0**(패키지 0.8.59 → 0.8.60). 백엔드는 `baro_calrory` 0.16.0 이 짝이다 —
**그쪽이 먼저 올라가야 이 화면이 동작한다**(`POST /api/cctv/probe-llm` 이 신설 라우트다).

백엔드에서 LLM 접속점이 `config.homing.visibilityVlm` 안에서 최상위 `config.llm` 으로 나왔다.
소비자가 둘이라(호밍의 번호판 가시성 판정 + 범용 통과 프록시 `/api/llm/v1/*`) 카드도 소비자가
아니라 **접속점 단위**로 뒀다 — 검출·판독 탭, URL / 기본 모델 / 타임아웃.

**버튼이 둘인 이유가 이 카드의 핵심이다.**

- 「연결 테스트」 — `mode` 를 안 보낸다. 백엔드가 `GET /v1/models` 만 읽어 **추론 슬롯을 안 쓴다.**
  그래서 서비스 상태 카드가 주기적으로 불러도 된다.
- 「동작 테스트」 — `mode:"run"`. 합성 이미지 + JSON 스키마로 실제 판정과 같은 모양을 1회 돈다.
  **추론을 실제로 쓴다.** 게이트웨이는 한 번에 하나만 돌리므로 도는 동안 버튼을 잠근다 —
  연타가 대기열을 채우면 진짜 판정이 밀린다.

도달성 하나로 합치면 **붙는데 못 쓰는 상태**(별칭 오타·vision 미지원·스키마 거절·warmup 미완)를
통과로 읽는다. 같은 이유로 서비스 상태 칩도 셋이다 — ❌ 못 붙음 / ⏳ 붙었지만 아직 추론 불가
(`r.ready === false`) / ✅ 준비됨. 둘로 뭉개면 화면이 "왜 안 되는지"를 말할 수 없다.

모델 칸은 datalist 다. **별칭은 모델명이 아니라 외워서 칠 수 없어서**, 연결 테스트가 쓸 수 있는
별칭을 채워 준다(값이 상류에서 오므로 문자열을 HTML 로 붙이지 않고 DOM 으로 만든다).
`llm.legacy` 가 참이면 "이 주소는 옛 homing.visibilityVlm 에서 빌려 온 것"이라고 말한다 —
저장한 적 없는 주소가 왜 떠 있는지를 화면이 설명해야 한다. 저장 한 번이면 이전된다.

계약은 테스트가 붙든다(`test/settings-calibration-pages.test.mjs`): 연결 핸들러에 `mode:` 가
없을 것, 동작 핸들러에 `mode:"run"` 과 `btn.disabled = true` 가 있을 것, 준비 안 된 상태를
따로 말할 것, 저장 payload 가 `llm` 을 실을 것. 새 element id 도 존재 검사 목록에 넣었다.
105개 통과.

### 2026-08-11 — 세워 둔 카메라의 설치를 고친다 · 설치 높이는 발행본에 있다

「높이를 편집하게 해 달라」에서 시작해, 그 값이 **어느 저장소의 사실인가**로 두 번 갈렸다.

#### 시뮬 카메라: 세우기와 값은 같지만 근거가 다르다

스폰 폼은 사람이 평면도에 찍은 두 점에서 방위·하향각을 만든다. 고치기는 출발점이 다르다 —
**씬이 보고한 지금의 설치**다. 그래서 x·y 는 손대지 않고 씬이 준 값을 그대로 되돌려보낸다.
축 하나만 보내면 나머지를 sim 이 0 으로 읽어 카메라가 원점으로 날아간다.

**하향각은 비워 두는 것이 「그대로」다.** 씬은 그 값을 주지 않는다(하향각은 PTZ tilt 라 카메라를
몰아 봐야 안다). 모르는 값을 폼에 채워 두면 손대지 않은 칸이 카메라를 돌린다. 대신 높이만 바꾸면
조준점이 멀어진다는 사실을 폼이 적어 둔다 — 둘을 어긋난 채로 두는 것도 사람의 선택이다.

레벨에 저작된 카메라(`spawned:false`)에는 편집 버튼을 만들지 않는다. 삭제와 같은 선이다.

#### 설치 높이는 config 가 아니라 발행본의 값이다

설정 › 기기 속성에 칸을 하나 더 놓았는데, 그 칸만 저장 경로가 다르다. 기기 config 왕복이 아니라
`POST /api/profiles/camera/:id/extrinsic` 로 나가고, 발행본은 불변이라 **저장이 곧 새 리비전**이다.
그래서 확인을 받고, 값이 그대로면 아무것도 보내지 않는다(안 그러면 저장 한 번에 리비전이 하나씩
쌓인다). 빈칸은 「지운다」가 아니다 — 발행본은 지울 수 없고 무르는 방법은 새 값을 발행하는 것뿐이다.
발행본이 없는 기기는 칸을 잠그고 이유를 말한다(높이는 곡선 **위에** 얹히는 값이라 백엔드가 거절한다).
순서는 프로파일 빌려오기 **다음** — 빌려온 그 리비전 위에 얹혀야 한다.

#### 배포본이 저장소보다 앞서 있었다

시작할 때 형제 저장소에 시뮬 카메라 쓰기 경로가 없어서(GET 하나뿐) "프론트가 없는 API 를 부른다"고
판단했는데, **현장 백엔드에는 이미 있었다.** 개발서버가 프록시하는 곳의 버전이 두 마이너 앞섰고
`POST·PATCH·DELETE /api/simulator/cameras` 가 다 문서화되어 있었다.

**교훈**: 저장소에 없다는 것과 배포에 없다는 것은 다른 사실이다. 계약을 「없다」고 단정하기 전에
지금 도는 백엔드의 `/api/help` 를 먼저 본다 — 그게 이 프론트가 실제로 말을 거는 상대다.

#### 목록이 조용히 낡는 문제

세우기·지우기는 **이 화면에서 한 것만** 즉시 반영됐다. 다른 브라우저에서 바꾸거나 sim 이
재시작하면(런타임 카메라는 전부 사라진다) 화면은 없는 카메라를 계속 보여 준다.

새로고침 버튼은 답이 못 된다 — 낡았다는 걸 아는 사람만 누를 수 있는데, 모르니까 안 누른다.
그래서 탭이 보이는 동안 씬을 다시 읽되 **바뀐 게 없으면 아무것도 다시 그리지 않는다**(5초마다
목록을 새로 만들면 스크롤과 커서가 그때마다 튄다). 배치 중·편집 중·조작 중에는 쉰다: `setBusy` 가
모든 버튼의 disabled 를 되돌리므로, 그 사이에 목록을 다시 만들면 잠가 둔 버튼이 되살아난다.

**기기 등록부는 같이 읽지 않는다.** `loadCameras` 는 활성 기기를 서버에 **쓰기**까지 하므로 주기
호출로 쓸 함수가 아니다. 등록부 변화는 탭을 열 때 잡는다.

#### 평면도가 「비뚤」어 보인 것은 그림이 아니라 레벨이었다

주차면이 계단처럼 밀려 보인다는 지적을 받고 씬 데이터를 쟀다. 한 열 여덟 면이 전부 yaw −87.51 이고
그 열이 실제로 뻗은 방향도 2.45° — **레벨에서 그 열이 2.49° 돌아가 있었다.** 나머지 네 그룹은 열
방향과 `yaw+90` 이 소수점까지 일치한다. 그림은 그것을 그대로 그리고 있었다(레벨 쪽 수정과 실측은
`baro_unreal` 의 dev_log 에 있다).

같은 자리에서 「폭이 겹친다」를 그림 결함으로 지목했다가 실측으로 물렀다: 5m 슬롯은 폭 256 × 깊이
500 이라 그림이 쓰는 폭 250 이 맞고, 1m 자리 옆의 177.8·169.8 간격은 어긋남이 아니라 좁은 자리가
낀 정상 배치였다(125 + 50 = 175). **화면을 의심하기 전에 재는 순서가 반대였다.**

낡은 문장 하나는 진짜 결함이라 고쳤다: 지도가 비었을 때만 쓰던 「씬에서 받은 주차면·카메라가
없습니다」를 렌더가 성공해도 되돌리지 않아, 24면이 그려진 지도 밑에서 화면이 아무것도 못 받았다고
말하고 있었다.

### 2026-08-05 (2차) — 백엔드 v0.10.1 대응: 발행 창구로 갈아탔다

형제 저장소가 캘리브레이션을 크게 정리해 올라왔다(0.8.2 → 0.10.1, 커밋 7개). 이쪽은 그
계약에 맞춰 따라간 세션이다. 코드보다 **판단이 바뀐 것**이 핵심이라 그쪽을 남긴다.

#### 같은 날 오전에 만든 기능이 오후에 틀린 경로가 됐다

「캘리브레이션 빌려오기」는 브라우저가 발행본을 읽어 곡선을 `config.devices[].intrinsics` 에
직접 써 넣는 방식이었다. 백엔드 커밋이 그걸 그대로 지목했다 — 그 우회로가 대상 카메라를
**"보정은 있는데 발행본은 없는" 상태**로 만들어 두 화면이 서로 다른 말을 하게 했다고.

지금은 `POST /api/profiles/camera/:id/copy` 가 정식 창구다. 백엔드 방식이 옳은 이유가 둘 있고,
둘 다 브라우저에서는 알 수 없는 것이었다.

- **복사는 광학만 옮기고 `device` 블록을 대상 기기 등록정보로 다시 찍는다.** 곡선은 렌즈의
  성질이지만 그것을 색인하는 눈금(줌축 범위·부호 규약·단위)은 프로토콜의 성질이다. 곡선만
  통째로 옮기면 `zoom:[0,65535]` 를 주장하는 문서가 줌축 `[100,1200]` 인 기기에 얹힌다.
- **`provenance.measuredOn` 이 복사의 복사를 지나 최초 실측 기기를 가리킨다.** 내가 지어
  붙이던 `label` 문자열보다 정직하다.

**교훈**: 프론트가 "백엔드가 안 해 주니 브라우저에서 조립하자"로 푼 문제는, 그 조립이 옳은지
판단할 정보가 브라우저에 없다는 점에서 대개 틀린 해법이다. 우회로를 만들기 전에 그 창구가
없는 게 맞는지 물어야 한다.

#### 구조화된 거절이 통째로 버려지고 있었다

`api.mjs` 가 오류 응답에서 `message` 만 뽑고 본문을 버렸다. 발행 게이트는
`code`·`failures[]`·`noisyAnchors[]` 로 **왜·어디가** 미달인지 말해 주는데 화면에는 문장
하나만 닿았고, 그러면 사람에게 남는 선택지가 20분 재측정뿐이다. `err.body` 한 줄로 살렸다.

**거절은 언제나 문장 하나가 아니다.** 오류 경로를 문자열로 좁히는 코드는 그 순간에는 단순해
보이지만, 서버가 구조를 주기 시작하면 그 단순함이 그대로 정보 손실이 된다.

#### 화면이 판단하지 말고 문서를 옮긴다

적용본(`intrinsics`)의 `measuredAt` 만 보고 "이 카메라에서 실측한 값"이라고 쓰면 복사본일 때
거짓말이 된다 — 그건 **원본 카메라**가 잰 시각이다. 그래서 `profileRevision` 으로 문서를 찾아
`provenance` 를 그대로 옮겨 말하게 고쳤다. 화면이 출처를 **추론**하지 않는다.

#### 이번에 붙인 것

- **발행 게이트 화면** — 미달 사유 목록 + 튀는 앵커(줌별) + 「그래도 발행」(확인 필수, 사유가
  문서에 영구히 남는다고 먼저 말한다). 통과했을 때도 튀는 앵커가 있으면 알려 준다.
- **피팅 오차 두 숫자** — 최댓값과 중앙값을 나란히. 게이트가 중앙값으로 옮겨간 이유가 화면에서
  읽혀야 한다("한 앵커가 시끄러웠다"와 "모델이 이 카메라를 설명 못 한다"는 다른 사실이다).
- **리비전 이력 + 되돌리기** — 발행본은 불변이고 정정은 새 리비전이라, 이 목록이 그 카메라의
  캘리브레이션 역사다. 되돌리기는 새 발행이 아니라 옛 리비전의 재적용(`/apply`)이다.
- **프로파일 목록 2단 배치** — 왼쪽은 저장소에 발행된 것 전량(카메라와 무관한 카탈로그),
  오른쪽은 고른 카메라의 작업면. 버튼 하나가 두 일을 한다: 이 카메라 것이면 **적용**,
  남의 것이면 **복사 발행 후 적용**. 결과는 같아 보여도 하나는 되돌리기이고 하나는 지워지지
  않는 리비전을 만드는 일이라, 버튼 이름과 확인 문구가 그 차이를 말한다.
- **route 정본화** — `/calibration/save` → `/calibration/mint`. save 는 하위호환 별칭이다.
- **i18n 중복 키 4개 제거 + 재발 금지 테스트.** 값이 같아 무해했지만 한쪽만 고치는 순간 그
  수정이 사라진 것처럼 보이고 원인이 사전 안에 있다. 테스트를 쓰다가 사전에 규약이 **둘**이라는
  것도 드러났다 — 한국어 원문 키(값에 ko 없음)와 `data-i18n-html` 식별자 키(값에 ko 있음).
  내 첫 테스트는 전자만 규약인 줄 알고 후자를 위반으로 잡았다. 테스트가 코드를 고치는 대신
  **테스트가 배웠다.**

#### 잔재 정리

`label` 기반 표시와 브라우저 곡선 조립을 걷어냈다. 살아 있는 기기 중 `label` 을 들고 있는 것이
없어 호환 분기는 남기지 않았다 — 없는 과거를 위한 호환층은 스스로 다음 잔재가 된다.

#### 설계 논의 (코드 없음)

- **참조 대신 복사 유지**(이교수님 확정). 참조는 원본 오염이다 — 떠 있는 참조는 원본 재측정이
  다른 카메라의 조준을 말없이 바꾸고, 고정 참조는 복사와 행동이 같다. 전파가 필요하면
  `provenance.from.revision` 대조로 「출처가 갱신됨」을 띄우고 재복사시키면 되고, 그건 백엔드
  수정이 필요 없다(아직 안 만들었다).
- **설치 측량은 백엔드 몫**이라고 정리했다. 소실점·삼각측량·강건추정은 도메인 수식이고, 이
  저장소는 "광학 모델을 두지 않는다"를 자기 원칙으로 갖고 있다. 프론트 몫은 커미셔너 표면뿐.
  계획은 형제 저장소 `_forAI/plan.md` 「설치 측량 에이전트」에 병합했다.

#### 남은 것

- 「출처가 갱신됨」 감지 + 재복사 버튼 (프론트만으로 가능)
- 설치 측량 에이전트의 프론트 표면 — 백엔드 S0 이 서고 나서
- `inventory.md` 의 `src/` 설명에 차트 모듈이 아직 안 들어갔다(지난 항목에서 이어짐)

### 2026-08-05 — 기기 편집기가 스키마 전부를 다룬다 · 캘리브레이션 값 다루기 · 화면 골격

첫 커밋 이후 첫 기능 세션. 큰 줄기는 셋이다 — 설정 화면의 기기 다루기, 캘리브레이션의 값
다루기, 화면 골격(헤더·이름) 정리.

#### 편집기가 못 채우는 필드는 고칠 수 없는 필드였다

이 세션에서 가장 값진 발견이다. 설정 화면에서 등록한 실기 한 대가 **연결 테스트는 통과하는데
화면은 안 나오는** 상태로 서 있었다. 백엔드에 직접 물어 두 갈래 실패를 확인했다.

- 스냅샷 500 — 평문 HTTP 만 여는 기기에 드라이버 기본값(https)이 TLS 핸드셰이크를 걸었다.
  `scheme` 이 없으면 이렇게 된다.
- 프리뷰 501 — 전역 `stream.rtspPath` 는 Hucoms 실측값이라 백엔드가 Hucoms 계열에만
  적용한다. 다른 벤더는 경로를 지어내지 않고 확답으로 501 을 준다(의도된 동작).
  기기별 `rtspPath` 가 없으면 프리뷰가 서지 않는다.

**둘 다 코드 결함이 아니라 빠진 설정이었고, 설정 화면에는 그 칸이 없었다.** 편집기가 다루는
필드가 백엔드 스키마의 부분집합이면, 그 차집합은 손으로 파일을 고쳐야만 닿는 값이 된다 —
UI 로 기기를 등록할 수 있다는 사실이 그 기기가 **동작하도록** 만들 수 있다는 뜻은 아니었다.
이제 편집기가 전부 다룬다: 용도(`mode`) · 스킴 · 프리뷰·전송 상세(rtsp 계열·스트림 포트·
타임아웃·TLS·채널 4종) · 가상 PTZ.

배치에서 정한 것 둘.

- **용도는 타입과 다른 축이다.** UE 시뮬은 hucoms 규약을 그대로 쓰므로 타입만으로 구분되지
  않는다. 시뮬레이터 페이지가 이 값으로 카메라를 고르니, 못 정하면 새로 등록한 시뮬
  카메라가 그 화면에 안 뜬다. 그래서 접힘 안이 아니라 **항상 보이는** 자리에 뒀다.
- **가상 PTZ 는 host 필드 바깥이다.** 백엔드가 이 값을 타입과 무관하게 읽기 때문이다.
  안에 두면 접속 대상 없는 타입에서 통째로 숨어 편집할 길이 사라진다.

#### 빈칸에는 뜻이 둘 있다 — 이 세션의 핵심 교훈

백엔드는 **안 보낸 필드를 보존**하고 **빈 문자열을 제거로** 읽는다. 이 비대칭 때문에, 응답이
아직 말해 주지 않는 필드를 화면이 빈칸으로 그리고 사람이 저장을 누르면 **멀쩡한 값이
지워진다**. "화면이 모른다"와 "사용자가 지웠다"가 같은 빈칸으로 보이기 때문이다.

해법은 원래 값을 기준으로 두 뜻을 가르는 것이다(`putField`): 입력이 비었고 **원래 값도
몰랐으면** 필드를 아예 싣지 않는다. 이 규칙 덕에 응답이 아직 부족한 백엔드에서도 안전하다 —
실기로 확인했다(새로고침 후 빈 칸인 채로 저장 → 스트림 200 유지).

같은 함정을 백엔드 쪽에서도 막았다: 형제 저장소의 `GET /api/cctv/config` 가 접속 필드
8개를 응답에서 빠뜨리고 있었다(`feat/cctv-config-roundtrip` 브랜치). 그 배포 전까지 이
필드들은 **쓸 수는 있지만 보이지는 않는다**.

#### 연결 테스트는 자기가 무엇을 봤는지 말해야 한다

`/api/cctv/probe` 는 Hucoms CGI 를 평문 HTTP 로, 쿼리스트링 인증으로 두드린다. 다른 규약
기기의 드라이버 경로(스킴·인증)와 무관하므로, "응답 있음"은 참이지만 화면과는 무관한 참이다.
사람은 그걸 "붙었다"로 읽는다. 통과했을 때 그 한계를 함께 표시하게 했다. **프로브를 타입별로
가르는 것은 백엔드 몫이고 아직 안 했다.**

#### 기기 CRUD 재설계

- **탭 분리**(기기/서버/검출·판독) → 한 줄로 늘어놓던 때는 찾는 데 스크롤이 필요했다.
- **2단 배치** — 왼쪽 목록, 오른쪽 편집, 스크롤은 단마다 따로. 접속 상세가 붙으면서 폼이
  화면 하나를 넘었고, 한 단이면 목록과 폼 사이를 계속 오가야 한다. 900px 아래는 한 줄.
  배치는 CSS 로 넘겼다 — **인라인 `flex-direction` 은 미디어쿼리까지 이긴다**(한 번 걸렸다).
- **행 자체가 선택이다** — 행마다 붙던 '편집' 버튼 제거(이름이 길면 줄바꿈이 나면서 버튼이
  세로로 찌그러졌다). 세 줄로 나눠 행 높이를 고정, 목록은 자기 안에서 스크롤.
- **조작은 한 줄에** — 추가·저장·삭제·취소·연결 테스트.
- **즉시 반영** — 확정 버튼이 브라우저 배열만 고치던 것을 서버 커밋으로 바꿨다. 이름이
  '적용'이라 누른 사람은 저장된 줄 알고 나갔고, 바로 옆 활성 기기 드롭다운은 즉시 반영이라
  한 카드 안에 규칙이 두 벌 있었다. 백엔드 저장이 항목별 부분 갱신이라 기기만 따로 보낸다.
- **순서를 끌어서** — 백엔드가 받은 차례 그대로 저장하므로 끌어 놓은 차례가 곧 저장 차례다.
  마우스 전용이다(키보드 경로 없음 — 버튼 2개를 더하면 "조작은 한 줄에"와 부딪힌다).

#### 캘리브레이션

- **메모 입력** — 백엔드가 `{note?}` 를 받아 발행본의 `quality.note` 로 굳히는데 화면에
  칸이 없었다. 문서화된 파라미터를 프런트가 안 쓰고 있던 셈이다.
- **발행 프로파일 가시화**(`src/profile-chart.mjs`, 신규) — 줌→화각, 줌→게인, 줌별 잔차와
  진단표. 라이브러리 없이 SVG 직접. 그리면서 확인한 것:
  - **한 그림에 축은 하나.** 각도(°)와 배율(×)은 같은 축에 못 얹는다.
  - 잔차 막대는 **범주형 배치**여야 한다 — 줌 값에 비례해 놓으면 망원 끝에서 겹친다.
  - 점마다 숫자를 찍지 않는다. 축 눈금이 이미 최소·최대를 말하고, 직접 라벨은 그 위에
    겹치기만 했다(그려 놓고 눈으로 보기 전에는 몰랐다).
- **다른 기기에 빌려주기** — 백엔드 수정 없이 됐다(`resolveIntrinsics` 가 실측 객체를 이미
  받는다). 설계에서 정한 것 둘:
  - **참조가 아니라 값 복사.** 발행본은 불변이지만 기기 설정은 바뀐다 — 참조면 원본 기기를
    지웠을 때 빌린 쪽이 조용히 보정을 잃는다.
  - **빌려온 값에는 출처 표식(`label`)을 남긴다.** 없으면 `measuredAt` 만 보고 화면이
    "이 카메라에서 실측한 값"이라고 **거짓말한다**. 판정 분기를 `label` 우선으로 고쳤다.
  - 적용 후 '검증'을 돌리라고 경고한다. 캘리브레이션은 개체의 성질이고, 오차가 원본보다
    작은 개체에 남의 보정을 씌우면 오히려 나빠진다.

#### 화면 골격

- 표기를 **Baro Calory** 로 통일(형제 저장소의 `c` 철자와 이 저장소의 `k` 는 그대로 —
  `README.md` 「현재 스냅샷」 참조).
- 헤더는 `홈(집 아이콘) | Baro Calory | 설정` 만. **API base 배지는 뺐다** — 주소는 설정
  화면이 말하고, 헤더가 매 페이지에서 반복할 이유가 없다. 배지가 사라지면서 헤더 nav 잠금
  루프도 잠글 것이 없어져 함께 걷어냈다(대문 카드 잠금은 그대로 — 홈은 언제나 탈출구다).
- 대문에서는 헤더에 설정 링크를 두지 않는다(화면 안의 카드가 이미 그 자리로 데려간다).

#### 남은 것

- **형제 저장소 `feat/cctv-config-roundtrip` 배포 전까지** rtsp 계열·채널 필드는 쓸 수는
  있어도 화면에 안 보인다.
- 연결 테스트를 기기 타입별로 가르는 일(백엔드).
- 새 기기 자동 id 가 `cam-###` 고정이라 기존 명명 관례와 어긋난다.
- 순서 바꾸기에 키보드 경로가 없다.
- `inventory.md` 의 `src/` 설명에 차트 모듈이 아직 안 들어갔다.

### 2026-08-04 (2차) — 이전 완료 · 최초 커밋

- **UI 전량이 옮겨 왔다.** 페이지 7 · 브라우저 모듈 11 · 테스트 7파일. 워크스페이스 밖에서
  독립 패키지로 성립하는 것까지 확인했다(`pnpm install` → `pnpm build` → `pnpm test` 59/59).
- **유출 감사 통과.** 사설 IP·실기 호스트명·계정명·형제 호스트 참조 0건. 「비밀번호」로
  걸리는 것은 전부 입력 필드와 i18n 문자열이지 값이 아니다. `git init` 을 새로 했으므로
  원본의 옛 이력(과거 실접속정보가 있던 시기)은 따라오지 않는다.
- **원본에서 제거됐다.** 형제 저장소의 `apps/frontend` 삭제 완료. 그쪽에는 이제 정적
  산출물도 빌드 단계도 없다.
- **마운트 프리픽스를 앱에서 걷어냈다.** 이 앱은 자기 오리진의 **루트**에 선다.
  `/barocalory` 는 예전에 프록시가 붙인 네임스페이스였을 뿐인데 앱의 사실인 양 코드에
  눌어붙어 있었다. 이 프로젝트는 리버스 프록시를 폐기했으므로(이교수님 확정) API 는 언제나
  교차 출처이고, 백엔드의 CORS 옵트인이 배포의 전제가 된다.
- **API 주소에 기본값을 두지 않기로 했다.** 분리 배포에서 "같은 곳에 있겠거니"는 틀린 쪽이
  정상이라, 미설정 상태를 그대로 드러낸다(배너 + 설정·대문 외 잠금). 설정 화면에 연결 테스트
  버튼을 붙여 주소를 넣고 바로 확인할 수 있게 했다.
- **프리뷰 관련 결함 셋을 고쳤다.**
  - **유령 fetch 경합**: `start()` 가 이전 `stop()` 을 await 하는 사이 새 요청이 끼어들어
    두 스트림이 겹쳤다. 세대 토큰(`gen`)으로 늦게 도착한 시작을 버린다.
  - **폴백 고착**: 스트림이 501 이면 스냅샷으로 내려앉는데, 그 상태가 **기기를 바꿔도** 안
    풀려 실기로 전환해도 계속 저fps 였다. 기기 전환 시 폴백 상태를 초기화한다.
  - **정지 화면의 깨진 이미지**: `src` 없는 `<img>` 를 브라우저가 깨진 링크 아이콘으로
    그렸다. 그리고 **대기(접속 시도 중)와 정지(안 켠 상태)를 뭉쳐 놓아서**, 프리뷰가 기본
    꺼짐인 페이지가 아무것도 기다리지 않으면서 `Wait...` 를 영원히 띄웠다. 두 상태를 갈라
    첫 페인트부터 「정지됨」을 중앙에 쓴다.
- **남은 것**: 정적 배포처 확정(혼합 콘텐츠가 실제 판단 기준) · 원격 저장소 연결.

### 2026-08-04 — 저장소 개설 · `_forAI` 스캐폴드

- 형제 저장소 `baro_calrory` 의 커미셔닝 콘솔 UI(`apps/frontend`)를 분리해 올 신규 저장소로
  이 디렉터리를 개설했다. 배포 형태는 **GitHub Pages 정적 호스팅**.
- `_forAI/` 문서 세트 5종을 생성했다(README · inventory · memo · dev_log · plan).
- 이 시점의 저장소는 **비어 있다** — UI 코드는 이전 전이고, git 저장소로 초기화되지도 않았다.
- 분리 결정의 배경: 같은 날 형제 저장소에서 **"배포에서 정적 서빙은 반드시 외부 서비스가
  담당한다"** 는 원칙이 확정됐다. 앱이 스스로 정적을 서빙하는 경로는 개발 도구일 뿐 배포
  서비스가 아니라는 것. 정적 산출물을 백엔드 저장소에서 떼어내는 것이 그 원칙의 연장이다.
- 공개 저장소이므로 이전 전에 유출 감사를 선행하기로 했다 — 내부망 주소·실기 호스트명·
  바이너리 자산(실촬영 여부)이 대상. 결과와 조치는 `plan.md`.
